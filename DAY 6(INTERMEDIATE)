#include <iostream>
using namespace std;

int main() {
	int t;
	cin>>t;
	while(t--){
	   int count=0;
		int d;
		cin>>d;
		char a[d];
		for(int i=0;i<d;i++)
		{
			cin>>a[i];
			if(a[i]=='5'||a[i]=='0')
			count++;
		}
		if(count==0)
		cout<<"No"<<endl;
		else
		cout<<"Yes"<<endl;
	}
	return 0;
}
